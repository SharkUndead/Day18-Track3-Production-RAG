# Mẫu file .env (Copy file này thành .env và điền các giá trị thực tế)

# Bắt buộc cho LLM & Enrichment (M5)
OPENAI_API_KEY=sk-...

# Tùy chọn (nếu dùng Cohere làm fallback)
COHERE_API_KEY=...

# Cấu hình Qdrant (Mặc định cho Local Docker)
QDRANT_HOST=localhost
QDRANT_PORT=6333
